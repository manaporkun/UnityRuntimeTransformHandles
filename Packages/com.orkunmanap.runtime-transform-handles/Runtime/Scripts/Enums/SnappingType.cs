namespace TransformHandles
{
    /// <summary>
    /// Defines the snapping behavior for handle transformations.
    /// </summary>
    public enum SnappingType
    {
        /// <summary>Snap to absolute world grid positions.</summary>
        Absolute,
        /// <summary>Snap based on relative movement increments.</summary>
        Relative
    }
}